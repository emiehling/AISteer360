"""AISteer360 agent package.

The agent runs on the user's machine, owns the GPU, and executes the three-stage pipeline against
a remote coordination server. See `agent.__main__` for the CLI entry point.
"""
